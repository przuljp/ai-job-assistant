# AI Job Assistant Frontend

The frontend uses React with Vite, React Router, and Axios.

## Development

```powershell
npm install
npm run dev
```

Copy `.env.example` to `.env` when a different backend URL is needed.
